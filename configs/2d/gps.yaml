defaults: ../base_2d.yaml

# GPS использует edge_attr через внутренний GINEConv.
# В 2D: edge_dim=7 (против 39 в 3D) → немного меньше параметров.
model:
  name: gps
  hidden: 124
  num_layers: 4
  heads: 4
  walk_length: 16
  dropout: 0.05
  n_params_target: 695667
